<% @messages.each do |message| %>
   <div class="message">
     <h3><%= message.title %> <small>(by: <%= message.author %>)</small></h3>		<p>
	<%= message.content %>
	<a href="/messages/new">Post a message... IT's FREE (for now)!</a>
	</p>
   </div>
<% end %>
